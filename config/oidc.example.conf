; Copy to /etc/disown/oidc.conf for production.
; Copy to /etc/disown/oidc-dev.conf for development.
; Keep this file disabled and never commit real secrets.

OIDC_ENABLED=0
OIDC_ISSUER=https://mein-iserv.de
OIDC_CLIENT_ID=your-client-id
OIDC_CLIENT_SECRET=change-me
OIDC_SCOPES="openid email profile iserv:roles iserv:uuid"
OIDC_ALLOWED_EMAILS="admin1@example.org,admin2@example.org"
OIDC_ALLOWED_ROLES="iPad-MDM-Admins"
