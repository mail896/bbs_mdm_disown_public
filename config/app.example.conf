; Copy to /etc/disown/app.conf.
; SERIAL_TOKEN_SECRET prepares signed WebClip URLs.
; Keep REQUIRE_SERIAL_TOKEN=0 until all existing WebClips include token=...

REQUIRE_SERIAL_TOKEN=0
SERIAL_TOKEN_SECRET=change-me-long-random-secret
