; Copy to /etc/disown/mail.conf.
; Used by PHPMailer for request completion mails and admin notifications.

MAIL_HOST=smtp.example.org
MAIL_PORT=587
MAIL_USERNAME=mdm@example.org
MAIL_PASSWORD=change-me
MAIL_FROM=mdm@example.org
MAIL_ENCRYPTION=tls
